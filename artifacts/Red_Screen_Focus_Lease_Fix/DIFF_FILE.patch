# PSVR extended-mode red screen / proximity focus lease fix

# --- src\display_component.cpp ---
diff --git "a/C:\\Users\\jm\\Documents\\psvr-steamvr\\artifacts\\Red_Screen_Focus_Lease_Fix\\original\\display_component.cpp" "b/src\\display_component.cpp"
index 4178ea8..e65be0d 100644
--- "a/C:\\Users\\jm\\Documents\\psvr-steamvr\\artifacts\\Red_Screen_Focus_Lease_Fix\\original\\display_component.cpp"
+++ "b/src\\display_component.cpp"
@@ -74,7 +74,7 @@ void PsvrDisplayComponent::GetWindowBounds(int32_t *pnX, int32_t *pnY, uint32_t
   *pnHeight = window_h_;
 }
 
-void PsvrDisplayComponent::PinCompositorWindow()
+void PsvrDisplayComponent::PinCompositorWindow(bool headset_worn)
 {
   HWND hwnd = FindWindowW(nullptr, L"Headset Window");
   if (!hwnd)
@@ -98,7 +98,8 @@ void PsvrDisplayComponent::PinCompositorWindow()
   GetWindowRect(hwnd, &r);
   const bool needs_layout = r.left != x || r.top != y ||
                             (r.right - r.left) != w || (r.bottom - r.top) != h;
-  if (needs_layout || !style_applied_)
+  const bool focus_state_changed = !have_focus_state_ || headset_worn_ != headset_worn;
+  if (needs_layout || !style_applied_ || focus_state_changed)
   {
     LONG_PTR style = GetWindowLongPtrW(hwnd, GWL_STYLE);
     style &= ~(WS_CAPTION | WS_THICKFRAME | WS_BORDER | WS_DLGFRAME | WS_OVERLAPPEDWINDOW);
@@ -106,18 +107,69 @@ void PsvrDisplayComponent::PinCompositorWindow()
     SetWindowLongPtrW(hwnd, GWL_STYLE, style);
 
     LONG_PTR ex = GetWindowLongPtrW(hwnd, GWL_EXSTYLE);
-    // A desktop extended-mode HMD window must stay positioned above the PSVR
-    // display, but it must never become the desktop's active input window.
-    ex |= WS_EX_TOPMOST | WS_EX_TOOLWINDOW | WS_EX_NOACTIVATE;
+    // SteamVR's extended-mode compositor deliberately renders solid red when
+    // its Headset Window is not foreground/fullscreen. Lease foreground only
+    // while the proximity sensor says the headset is being worn. Otherwise
+    // make the window non-activating so SteamVR cannot disrupt desktop input.
+    ex |= WS_EX_TOPMOST | WS_EX_TOOLWINDOW;
+    if (headset_worn)
+      ex &= ~WS_EX_NOACTIVATE;
+    else
+      ex |= WS_EX_NOACTIVATE;
     ex &= ~(WS_EX_WINDOWEDGE | WS_EX_APPWINDOW);
     SetWindowLongPtrW(hwnd, GWL_EXSTYLE, ex);
 
     SetWindowPos(hwnd, HWND_TOPMOST, x, y, w, h,
-                 SWP_FRAMECHANGED | SWP_SHOWWINDOW | SWP_NOOWNERZORDER | SWP_NOACTIVATE);
+                 SWP_FRAMECHANGED | SWP_SHOWWINDOW | SWP_NOOWNERZORDER |
+                     (headset_worn ? 0 : SWP_NOACTIVATE));
     style_applied_ = true;
+    have_focus_state_ = true;
+    headset_worn_ = headset_worn;
     DriverLog("PSVR: pinned Headset Window from (%d,%d %dx%d) to (%d,%d %dx%d)",
               r.left, r.top, r.right - r.left, r.bottom - r.top, x, y, w, h);
   }
+
+  const HWND foreground = GetForegroundWindow();
+  if (headset_worn)
+  {
+    if (foreground == hwnd)
+      return;
+    if (foreground && IsWindow(foreground))
+      previous_foreground_ = reinterpret_cast<uintptr_t>(foreground);
+
+    const DWORD current_thread = GetCurrentThreadId();
+    const DWORD target_thread = GetWindowThreadProcessId(hwnd, nullptr);
+    const DWORD foreground_thread = foreground ? GetWindowThreadProcessId(foreground, nullptr) : 0;
+    if (target_thread && target_thread != current_thread)
+      AttachThreadInput(current_thread, target_thread, TRUE);
+    if (foreground_thread && foreground_thread != current_thread && foreground_thread != target_thread)
+      AttachThreadInput(current_thread, foreground_thread, TRUE);
+
+    ShowWindow(hwnd, SW_SHOW);
+    BringWindowToTop(hwnd);
+    SetWindowPos(hwnd, HWND_TOPMOST, x, y, w, h, SWP_SHOWWINDOW | SWP_NOOWNERZORDER);
+    SetForegroundWindow(hwnd);
+    SetActiveWindow(hwnd);
+    SetFocus(hwnd);
+    const LPARAM center = MAKELPARAM(w / 2, h / 2);
+    PostMessageW(hwnd, WM_LBUTTONDOWN, MK_LBUTTON, center);
+    PostMessageW(hwnd, WM_LBUTTONUP, 0, center);
+
+    if (foreground_thread && foreground_thread != current_thread && foreground_thread != target_thread)
+      AttachThreadInput(current_thread, foreground_thread, FALSE);
+    if (target_thread && target_thread != current_thread)
+      AttachThreadInput(current_thread, target_thread, FALSE);
+    DriverLog("PSVR: Headset Window fullscreen focus acquired (worn=1)");
+  }
+  else if (foreground == hwnd && previous_foreground_)
+  {
+    const HWND previous = reinterpret_cast<HWND>(previous_foreground_);
+    if (IsWindow(previous))
+    {
+      SetForegroundWindow(previous);
+      DriverLog("PSVR: desktop focus restored (worn=0)");
+    }
+  }
 }
 
 bool PsvrDisplayComponent::IsDisplayOnDesktop()

# --- src\display_component.h ---
diff --git "a/C:\\Users\\jm\\Documents\\psvr-steamvr\\artifacts\\Red_Screen_Focus_Lease_Fix\\original\\display_component.h" "b/src\\display_component.h"
index b51f85a..0a69148 100644
--- "a/C:\\Users\\jm\\Documents\\psvr-steamvr\\artifacts\\Red_Screen_Focus_Lease_Fix\\original\\display_component.h"
+++ "b/src\\display_component.h"
@@ -3,6 +3,8 @@
 #include "openvr_driver.h"
 #include "psvr_hw.h"
 
+#include <cstdint>
+
 struct PsvrOptics
 {
   // PSVR panel is 63mm x 71mm per eye, lens ~35.4mm away, slightly high.
@@ -32,7 +34,7 @@ public:
 
   int RefreshHz() const { return refresh_hz_; }
   bool FoundRealDisplay() const { return real_display_; }
-  void PinCompositorWindow();
+  void PinCompositorWindow(bool headset_worn);
   int WindowX() const { return window_x_; }
   int WindowY() const { return window_y_; }
   uint32_t WindowW() const { return window_w_; }
@@ -48,5 +50,8 @@ private:
   int refresh_hz_ = 60;
   bool real_display_ = false;
   bool style_applied_ = false;
+  bool have_focus_state_ = false;
+  bool headset_worn_ = false;
+  uintptr_t previous_foreground_ = 0;
   PsvrOptics optics_{};
 };

# --- src\hmd_device.cpp ---
diff --git "a/C:\\Users\\jm\\Documents\\psvr-steamvr\\artifacts\\Red_Screen_Focus_Lease_Fix\\original\\hmd_device.cpp" "b/src\\hmd_device.cpp"
index ece1bfd..14cdaf8 100644
--- "a/C:\\Users\\jm\\Documents\\psvr-steamvr\\artifacts\\Red_Screen_Focus_Lease_Fix\\original\\hmd_device.cpp"
+++ "b/src\\hmd_device.cpp"
@@ -131,6 +131,8 @@ void PsvrHmdDevice::Deactivate()
     hotkey_thread_.join();
   if (pose_thread_.joinable())
     pose_thread_.join();
+  if (display_)
+    display_->PinCompositorWindow(false);
   hw_.SetVrMode(false);
   hw_.Close();
   object_id_ = vr::k_unTrackedDeviceIndexInvalid;
@@ -232,12 +234,13 @@ void PsvrHmdDevice::HotkeyThread()
 
 void PsvrHmdDevice::RunFrame()
 {
+  const PsvrPose hw = hw_.GetPose();
   // RunFrame executes on vrserver's driver loop and can see compositor HWNDs;
-  // the pose worker thread can live on a non-interactive window station.
+  // the pose worker thread can live on a non-interactive window station. The
+  // proximity-gated focus lease keeps extended-mode video active only in use.
   if (display_)
-    display_->PinCompositorWindow();
+    display_->PinCompositorWindow(hw.worn);
 
-  const PsvrPose hw = hw_.GetPose();
   const uint8_t pressed = hw.buttons & ~last_buttons_;
   last_buttons_ = hw.buttons;
   if (pressed)

# --- src\hmd_device.h ---

# --- src\psvr_hw.cpp ---
diff --git "a/C:\\Users\\jm\\Documents\\psvr-steamvr\\artifacts\\Red_Screen_Focus_Lease_Fix\\original\\psvr_hw.cpp" "b/src\\psvr_hw.cpp"
index 4b831b8..b7d1b3f 100644
--- "a/C:\\Users\\jm\\Documents\\psvr-steamvr\\artifacts\\Red_Screen_Focus_Lease_Fix\\original\\psvr_hw.cpp"
+++ "b/src\\psvr_hw.cpp"
@@ -491,7 +491,10 @@ void PsvrHardware::HandleSensorPacket(const uint8_t *buffer, int size)
   std::lock_guard<std::mutex> lock(pose_mutex_);
   pose_.rotation = oriented;
   pose_.connected = true;
-  pose_.worn = worn || proximity > 300;
+  // The sensor report's state bit is the debounced on-head signal. The raw
+  // 16-bit proximity field rests around 39k on this unit, so treating values
+  // above 300 as "worn" made SteamVR seize focus for the entire session.
+  pose_.worn = worn;
   pose_.hdmi_ok = !hdmi_disconnected;
   pose_.buttons = buttons;
   pose_.proximity = proximity;
